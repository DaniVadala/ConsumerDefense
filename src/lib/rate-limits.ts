/** Max conversations per IP per day that ended as non-conducive (before blocking new starts) */
export const MAX_NC_CONVERSATIONS = 2;

/** Max conversations per IP per day that ended as insulto or absurdo (before blocking new starts) */
export const MAX_ABUSIVE_CONVERSATIONS = 1;
